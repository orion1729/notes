
1. 不使用暫存變數交換兩個變數得值
	利用 XOR

2. 找第 K 大的值
	當 K 很小時可以利用多個變數把最大、第二大等值依序紀錄並更新，
	可以有 O(n) 的效率。
	而 K 很大時可利用 Quick Sort 來排序，效率為 O(n logn)

3. 在 n 個連續整數中找到缺少了哪一個
	利用梯形公式找出 n 個連續整數的和，再與所輸入的相減即可找到

4. 從兩個數字中找到一個比較大的，但不使用判斷描述
	利用相減後再 shift 找到 signed bit，利用此值不是 0 或 1 的特性
	額外創造一個只有雙元素的 array，兩者搭配起來即可找到

5. 